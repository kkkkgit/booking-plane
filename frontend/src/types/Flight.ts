export interface Flight {
    id: number;
    destination: string;
    departureDate: string;
    departureTime: string;
    arrivalDate: string;
    arrivalTime: string;
    price: number;
    flightDuration: string;
}